import SignatureMaker from '@/components/admin/SignatureMaker';

export const metadata = { title: 'Signature Maker | IBF Admin' };

export default function SignatureMakerPage() {
  return <SignatureMaker />;
}
