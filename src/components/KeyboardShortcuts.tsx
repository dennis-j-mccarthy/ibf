'use client';

// Keyboard shortcuts are now handled directly in VersionContext
// This component is kept for backwards compatibility but does nothing
// Press 'c' for Catholic mode, 'p' for Public mode (when not in an input field)

export default function KeyboardShortcuts() {
  return null;
}
